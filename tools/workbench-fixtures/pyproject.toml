[project]
name = "tabloom-workbench-fixtures"
version = "0.1.0"
description = "Offline generator and validator for the Tabloom workbench v1 fixture package"
requires-python = ">=3.12,<3.13"
dependencies = [
  "duckdb>=1.4,<2",
  "pyarrow==25.0.1",
]

[tool.uv]
package = false
