export interface ModelCacheEvidence { readonly provider: "wasm" | "webgpu"; readonly precision: "fp32" | "fp16-storage"; readonly firstDownloadBytes: number; readonly secondDownloadBytes: number; readonly applicationCacheHit: boolean; readonly independentMeanChecked: boolean; }
export function assertApplicationCache(evidence: ModelCacheEvidence): void { if (evidence.secondDownloadBytes !== 0 || !evidence.applicationCacheHit) throw new Error("Model cache was not reused by application storage"); if (!evidence.independentMeanChecked) throw new Error("Model cache suite lacks independent mean evidence"); }
