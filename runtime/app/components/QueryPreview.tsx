export function renderQueryPreview(columns: readonly string[], rows: readonly Record<string, unknown>[], total: number): string { return `<section aria-label="查询预览"><span>${total} rows</span><table><thead><tr>${columns.map((column) => `<th>${escapeHtml(column)}</th>`).join("")}</tr></thead><tbody>${rows.slice(0, 200).map((row) => `<tr>${columns.map((column) => `<td>${escapeHtml(String(row[column] ?? "NULL"))}</td>`).join("")}</tr>`).join("")}</tbody></table></section>`; }
function escapeHtml(value: string): string { return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;"); }
