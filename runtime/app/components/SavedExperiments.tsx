export interface SavedExperimentView { readonly id: string; readonly revision: number; readonly name: string; readonly status?: string; }
export function renderSavedExperiments(values: readonly SavedExperimentView[]): string { return `<section class="saved-experiments" aria-label="已保存实验"><div class="section-title">已保存实验</div>${values.length ? `<ul>${values.map((value) => `<li><button type="button" class="saved-experiment" data-experiment="${escapeHtml(value.id)}" data-revision="${value.revision}"><strong>${escapeHtml(value.name)}</strong><span>revision ${value.revision}${value.status ? ` · ${escapeHtml(value.status)}` : ""}</span></button></li>`).join("")}</ul>` : `<p class="muted">暂无保存的实验</p>`}</section>`; }
function escapeHtml(value: string): string { return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;"); }
