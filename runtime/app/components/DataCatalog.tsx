export interface CatalogViewModel { readonly tableName: string; readonly rows: number; readonly columns: readonly { readonly name: string; readonly type: string; readonly nulls: number; readonly min?: number | null; readonly max?: number | null; readonly mean?: number | null }[]; }
export function renderDataCatalog(model: CatalogViewModel): string { return `<section aria-label="数据目录"><h2>${escapeHtml(model.tableName)}</h2><p>${model.rows} 行 · ${model.columns.length} 列</p><ul>${model.columns.map((column) => `<li><strong>${escapeHtml(column.name)}</strong> <span>${escapeHtml(column.type)} · NULL ${column.nulls}</span></li>`).join("")}</ul></section>`; }
function escapeHtml(value: string): string { return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;"); }
